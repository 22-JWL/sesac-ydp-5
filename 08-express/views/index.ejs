<!-- //javascript를 쓸 수 있는 html = ejs -->
<!-- 변경후 서버 껐다 켤 필요 없이 새로고침만 하면 적용된다! -->
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>메인 페이지</title>
    <%- include('./includes/head', {title: '흠'}) %>
    <link rel="stylesheet" href="/public/css/home.css">
  </head>
  <body>
    <%- include('./includes/header.ejs') %>
    <h1>Hello Express!</h1>
    <p>hello! <%= userId%>님 환영합니다,</p>

    <p>혹시 pw가 <%= userPw%>인가요?</p>
    <p>hello! <%= userId%>님 환영합니다,</p>

    <% for (let i =1; i<=3; i++){ %>
      <button type="button" class ="btn btn-primary"><%= btns[i] %>
    <% } %>

    <br/>
    <span><%= me.name %></span>
    <span><%= me.msg %></span>
  </body>
</html>
